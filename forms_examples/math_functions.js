export default function subtract(x, y) {return x - y;}  // I'm just going to export this one thing from the file.
        
// So, now we know that this is just the fallback (резервный, "аварийный режим"). Basically, just the one thing we're going to 
// export from this file.